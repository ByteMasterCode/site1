export type Root = SliderModel[]

export interface SliderModel {
    id: number
    title: string
    content: string
    category: string
    image: string
    created_at: string
    updated_at: string
}
